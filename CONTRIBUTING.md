# 贡献指南

谢谢你愿意来完善孵梦 🌙

## 开发方式

**没有构建步骤。** 整个产品就是 `index.html`。改完直接用浏览器打开看效果。

```bash
python3 -m http.server 8099
# 打开 http://localhost:8099
```

建议用支持 HTTPS 的方式自测录音功能（见 README）。

## 设计原则（请尽量遵守）

1. **单文件、零依赖、纯前端。** 不要引入框架、打包器或外部 CDN。这是这个项目的底线 —— 它存在的意义就是「一个链接就能用」。
2. **本地优先。** 数据默认只存在浏览器。任何需要后端的改动，请先在 issue 里讨论。
3. **不替用户花钱。** AI、视频等付费能力做成「用户自带 Key / 只给提示词」，不要把费用压到产品侧。
4. **暗色、安静、不打扰。** 使用场景多在半睡半醒之间，界面要克制。

## 怎么提 PR

1. Fork → 建分支 → 改 → 提 PR。
2. 如果改动较大，先开一个 issue 说清楚想做什么，避免重复劳动。
3. PR 里写清楚：改了什么、为什么、怎么测的。

## 适合新人的任务

见 [docs/ROADMAP.md](docs/ROADMAP.md) 里标了 `good first issue` 的条目。

## 行为准则

请友善、耐心。我们面对的是人们最私密也最脆弱的素材 —— 梦。
